; Exercise 1.34

;; The same as if you tried (f 2), i.e. the interpreter attempts to call 'f'
;; with an argument of 2, which it then attempts to call with an argument of
;; 2. But 2 is not a function, so the interpreter complains and the code is
;; not executed.

