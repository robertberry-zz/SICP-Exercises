(load "ex414.scm")

; Exercise 4.1.5

; If 'try' halts when given 'try', then 'try' by its own definition does not
; halt, it runs forever.

; If 'try' does not halt when given 'try', then by its own definition it does
; halt.

